import React from 'react';
import { ThemedView } from './ThemedView';
import { ThemedText } from './ThemedText';

export default function SessionView() {
  return (
    <ThemedView>
      <ThemedText>welcome to the session</ThemedText>
    </ThemedView>
  )
}